[unreleased]
 - add preprocessor functional interfaces
  Preprocessors can be used to perform checks during search algorithms and decide whether to continue processsing the vertex.
  These replace the maxDepth and maxDistance arguments.
